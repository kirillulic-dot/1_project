{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Отладка project.exe",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}\\project.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${fileDirname}",
      "externalConsole": true,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "preLaunchTask": "build"
    }
  ]
}
